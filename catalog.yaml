description: 'NOC Master Data Catalog'
sources:

  ARCHER:
    args:
      path: "{{ CATALOG_DIR }}/ARCHER/catalog-ARCHER.yaml"
    description: ARCHER Dataset Catalog
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  JASMIN:
    args:
      path: "{{ CATALOG_DIR }}/JASMIN/catalog-JASMIN.yaml"
    description: JASMIN Dataset Catalog
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  NOCS:
    args:
      path: "{{ CATALOG_DIR }}/NOCS/catalog-NOCS.yaml"
    description: NOCS Dataset Catalog
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
  
  THREDDS:
    args:
      path: "{{ CATALOG_DIR }}/THREDDS/catalog-THREDDS.yaml"
    description: THREDDS Dataset Catalog
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
